import { heartFailure } from '../courses/heart-failure';
import { coronaryDisease } from '../courses/coronary-disease';

export const diseases = {
  id: 'cardio-diseases',
  title: 'Maladies',
  courses: [heartFailure, coronaryDisease]
};